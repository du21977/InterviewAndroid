<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dobi.interviewandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".fragment.FragmentTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- <activity android:name=".MainActivity" /> -->
        <activity android:name=".MainActivity" />
        <activity
            android:name=".webview.WebviewActivity"
            android:process=".webview" />
        <activity android:name=".webview.Webview2Activity" />
        <activity android:name=".handler.HandlerActivity" />
        <activity android:name=".handler.Handler2Activity" />
        <activity android:name=".asynctask.AsyncTaskActivity" />
        <activity android:name=".asynctask.AsyncTask2Activity" />
        <activity android:name=".handlerthread.HandlerThreadActivity" />
        <activity android:name=".performance.Main2Activity" />
        <activity android:name=".performance.leak.animation.LeakAnimationActivity"></activity>
    </application>

</manifest>